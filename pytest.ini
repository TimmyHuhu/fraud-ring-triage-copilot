[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q
# Put the repo root on sys.path so `import agents` works under the bare
# `pytest` console script (not just `python -m pytest`).
pythonpath = .
